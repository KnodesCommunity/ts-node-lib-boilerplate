/**
 * This is a sample integration test file. It is used to test the whole application/library behavior when using all of its components.
 *
 * @kind DEMO FILE
 */

import { Foo } from '~/example';

it( 'Spec', () => {
	expect( new Foo() ).toBeInstanceOf( Foo );
} );
